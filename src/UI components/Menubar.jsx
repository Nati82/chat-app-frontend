import { Box } from "@mui/material";

const Menubar = () => {
  return (
    <Box
      flex={1}
      bgcolor="magenta"
      sx={{ display: { xs: "none", sm: "block" } }}
    >
      ;Menubar
    </Box>
  );
};

export default Menubar;
